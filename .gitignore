# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Jira sync pipeline artifacts: raw pulls and auto-derived clustering
# output, both regenerated fresh on every sync run (see
# scripts/jira-sync/run-full-sync.mjs) -- not app source, not meant to be
# committed. Manual corrections belong in the DB (project_overrides,
# people.team_guessed), not in these regenerated files.
scripts/jira-sync/cache/
src/data/generated/

.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Netlify folder
.netlify
